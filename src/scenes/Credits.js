export default class Credits extends Phaser.Scene {
    constructor(config) {
        super(config);
    }
    preload() {}
    create() {
        console.log("Welcome to the credits scene");
    }
}
